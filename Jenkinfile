

pipeline {
  agent any

  stages {

    stage('Build Image') {
      steps {
        dir("springboot-backend"){
           sh "sudo usermod -a -G docker jenkins"
           sh "docker build -t salman ."
        }

      }
    }

  }
}
