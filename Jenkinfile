

pipeline {
  agent any

  stages {

    stage('Build Image') {
      steps {
        dir("springboot-backend"){
           sh "usermod -a -G docker jenkins"
           sh "docker build -t salman ."
        }

      }
    }

  }
}
